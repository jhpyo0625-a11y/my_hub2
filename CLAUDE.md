# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project status

`my_hub2` is a fresh scaffold. `backend/`, `frontend/`, and `mcp/` directories exist but are empty — no code, no package manifests, no build tooling has been set up yet. There are no commands to build, lint, or test because nothing has been implemented.

When adding the first code to one of these directories, set up the appropriate tooling (package manager, test runner, etc.) for that stack and update this file with the real commands.
